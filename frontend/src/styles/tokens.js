// Shared style tokens & inline style helpers
export const mono = { fontFamily: "var(--font-mono)" };

export const card = {
  background: "var(--surface)",
  border: "1px solid var(--border)",
  borderRadius: 12,
};
